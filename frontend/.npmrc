# pnpm 11+: disable the implicit pre-run install verification (which re-fails the
# "approve build scripts" gate every time we invoke `pnpm dev`) and turn off
# strict gating for known-safe deps like sharp (prebuilt binaries via
# @img/sharp-win32-x64).
verify-deps-before-run=false
strict-dep-builds=false
