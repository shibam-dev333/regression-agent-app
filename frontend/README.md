# Frontend — Next.js 15

```powershell
cd frontend
pnpm install
pnpm dev
```

Opens at http://localhost:3000. Connects to the backend WebSocket at
`NEXT_PUBLIC_BACKEND_WS` (default `ws://localhost:8000`).
